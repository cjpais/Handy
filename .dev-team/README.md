# .dev-team/

Scaffolding for the autonomous dev-team workflow. CONTEXT.md = project KB (accreted by DevOps role on merge).
Per-feature dirs `<feature-id>/` contain spec.md / tech-spec.md / tasks.md / discovery.md / eval-*.md / qa-report.md / deploy-log.md (added on the feature PR, NOT this bootstrap PR).
